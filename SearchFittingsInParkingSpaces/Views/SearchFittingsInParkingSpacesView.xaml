<Window
    x:Class="SearchFittingsInParkingSpaces.Views.SearchFittingsInParkingSpacesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:viewModel="clr-namespace:SearchFittingsInParkingSpaces.ViewModels"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance viewModel:SearchFittingsInParkingSpacesViewModel}"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterScreen"
    Title="Поиск арматуры над парковочным местом"
    Height="450"
    Width="800">
    <Grid>
        <StackPanel>
            <Button Content="Найти арматуру над парковочными местами"
                    Command="{Binding FindFittingsCommand}"
                    Margin="0,0,0,10"
                    Width = "300"/>
            <TextBlock Text = "Найденные элементы:" FontWeight="Bold" Margin="0,10,0,5"/>
            
            <ListBox ItemsSource="{Binding Fittings}"
                     SelectedItem="{Binding SelectedFitting, Mode=TwoWay}"
                     Height="300"/>
        </StackPanel>
    </Grid>
</Window>